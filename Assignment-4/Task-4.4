# -------------------- ZERO-SHOT PROMPT --------------------
# Prompt: Write a Python function that counts the number of vowels in a string.
def count_vowels(s: str) -> int:
    """Return the number of vowels in the input string."""
    vowels = "aeiouAEIOU"
    return sum(1 for char in s if char in vowels)
# Example usage:
input_string = "Hello, World!"
vowel_count = count_vowels(input_string)
print(f"Number of vowels in '{input_string}': {vowel_count}")
# -------------------- END OF ZERO-SHOT PROMPT --------------------
# -------------------- FEW-SHOT PROMPT --------------------
# Prompt: Write a Python function that counts the number of vowels in a string.
def count_vowels(s: str) -> int:
    """Return the number of vowels in the input string."""
    vowels = "aeiouAEIOU"
    return sum(1 for char in s if char in vowels)   
# Example usage:
input_string ="pythonic"
vowel_count = count_vowels(input_string)
print(f"Number of vowels in '{input_string}': {vowel_count}")
# -------------------- END OF FEW-SHOT PROMPT --------------------
